void scopes(int k, float k){
   vec2 k;

   for(k = 0; k < 5; ++k){
        vec3 k;

        while(k)
            bool k;
        for(k = 0; true; k++)
            int k;

        while(k){
            vec4 k;

            if(k)
                mat2 k;
            else {
                mat3 k;

                switch(5){
                    case 2:
                        vec2 k;
                        {
                           vec3 k;
                        }
                    case 3:
                        vec4 k;
                    default:
                        float k;
                }
            }
            mat4 k;
        }
        bool k;
   }
   int k;
}
